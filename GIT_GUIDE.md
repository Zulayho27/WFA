# Файлы, которые НЕ должны попадать в GitHub

## ✅ Уже в .gitignore:

1. **node_modules/** - зависимости (очень большие!)
2. **package-lock.json** - автогенерируемый файл
3. **.env** - КРИТИЧНО! Содержит пароли БД и секреты
4. **backend/uploads/** - загруженные пользователями изображения
5. **logs/** и **.log** - логи приложения
6. **.vscode/** - настройки редактора
7. **dist/**, **build/** - скомпилированный код

## ⚠️ ВАЖНО перед push:

### Проверьте .env файл:
```bash
# Убедитесь что .env НЕ будет загружен:
git status
```

Если видите `.env` в списке - это ОПАСНО! Немедленно:
```bash
git rm --cached backend/.env
git rm --cached frontend/.env
```

### Создайте .env.example:
Вместо реального .env создайте шаблон без секретов:

**backend/.env.example:**
```
PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
JWT_SECRET=your_secret_key_here
```

## 📋 Что ДОЛЖНО быть в GitHub:

✅ Исходный код (.js, .jsx, .css)
✅ package.json (без package-lock.json)
✅ README.md
✅ .gitignore
✅ .env.example (шаблон без секретов)
✅ SQL схемы базы данных
✅ Конфигурационные файлы

## 🚀 Перед первым push:

```bash
# 1. Инициализация git (если еще не сделано)
git init

# 2. Проверка что игнорируется
git status

# 3. Добавление файлов
git add .

# 4. Коммит
git commit -m "Initial commit: World Food Atlas"

# 5. Подключение к GitHub
git remote add origin https://github.com/ваш-username/world-food-atlas.git

# 6. Push
git push -u origin main
```

## ⚡ Быстрая проверка:

```bash
# Покажет что будет загружено (должно быть ~50-100 файлов, не тысячи!)
git ls-files | wc -l
```

Если видите тысячи файлов - значит node_modules попал в git!
