# Server Configuration
PORT=5000
NODE_ENV=development

# Supabase Database Connection
# Use ANY of these variable names (code will check all of them):
# Option 1: Standard Supabase variable name
POSTGRES_URL=your_supabase_postgres_url_here

# Option 2: Supabase Prisma URL (alternative)
# POSTGRES_PRISMA_URL=your_supabase_prisma_url_here

# Option 3: Standard DATABASE_URL (also works)
# DATABASE_URL=your_connection_string_here

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# INSTRUCTIONS:
# 1. Copy POSTGRES_URL from your Supabase dashboard (shown in your screenshot)
# 2. Paste it above, replacing "your_supabase_postgres_url_here"
# 3. Save this file as .env (remove .example from filename)
# 4. Restart the server with: npm run dev
