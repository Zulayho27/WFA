# 🚀 Решение проблемы с Vercel - Два проекта из одного репозитория

## Проблема
Vercel позволяет деплоить только один проект из репозитория.

## ✅ Решение: Root Directory

Vercel ПОЗВОЛЯЕТ создать несколько проектов из одного репозитория, если указать разные Root Directory!

### 📋 Пошаговая инструкция:

---

## ПРОЕКТ 1: Backend

1. **Vercel Dashboard** → "Add New Project"

2. **Import Git Repository:**
   - Выберите `WFA` из GitHub

3. **Configure Project:**
   - Project Name: `wfa-backend` (или любое имя)
   - Framework Preset: `Other`
   
4. **⚠️ ВАЖНО - Root Directory:**
   - Нажмите кнопку **"Edit"** рядом с Root Directory
   - Выберите папку **`backend`**
   - Или введите вручную: `backend`

5. **Build Settings:**
   - Build Command: `npm install`
   - Output Directory: `.` (точка)
   - Install Command: `npm install`

6. **Environment Variables:**
   ```
   PORT = 5000
   DB_HOST = ваш-postgresql-хост
   DB_PORT = 5432
   DB_NAME = world_food_atlas
   DB_USER = ваш-username
   DB_PASSWORD = ваш-пароль
   JWT_SECRET = ваш-секретный-ключ
   NODE_ENV = production
   ```

7. **Deploy** → Скопируйте URL (например: `https://wfa-backend.vercel.app`)

---

## ПРОЕКТ 2: Frontend

1. **Vercel Dashboard** → "Add New Project" (СНОВА!)

2. **Import Git Repository:**
   - Выберите **ТОТ ЖЕ** репозиторий `WFA`
   - Vercel позволит это сделать!

3. **Configure Project:**
   - Project Name: `wfa-frontend` (другое имя!)
   - Framework Preset: `Vite`
   
4. **⚠️ ВАЖНО - Root Directory:**
   - Нажмите **"Edit"**
   - Выберите папку **`frontend`**
   - Или введите: `frontend`

5. **Build Settings:**
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

6. **Environment Variables:**
   ```
   VITE_API_URL = https://wfa-backend.vercel.app
   ```
   (Используйте URL из шага 1!)

7. **Deploy**

---

## 🎬 Визуальная подсказка:

При создании проекта вы увидите:

```
┌─────────────────────────────────────┐
│ Configure Project                   │
├─────────────────────────────────────┤
│ Project Name: wfa-backend           │
│ Framework: Other                    │
│ Root Directory: ./  [Edit] ← ЖМИ!  │
│                                     │
│ После клика на Edit:                │
│ ┌─────────────────────────────┐    │
│ │ Select Root Directory       │    │
│ │ ○ ./ (root)                 │    │
│ │ ● backend/  ← ВЫБЕРИ ЭТО!   │    │
│ │ ○ frontend/                 │    │
│ └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

---

## 🔄 Альтернатива: Render.com

Если Vercel все равно не работает, используйте **Render.com**:

**Преимущества:**
- ✅ Автоматически определяет monorepo
- ✅ Бесплатный PostgreSQL включен
- ✅ Проще настройка
- ✅ Один сервис для всего

**Минусы:**
- ⏱️ Медленнее cold start (засыпает после 15 мин)
- 🕐 750 часов/месяц бесплатно

### Быстрый старт с Render:

1. Зайдите на https://render.com
2. "New" → "Web Service"
3. Подключите GitHub репозиторий
4. Render автоматически найдет backend и frontend
5. Создайте PostgreSQL базу (бесплатно)
6. Deploy!

---

## ❓ Если не получается

Покажите мне скриншот экрана где Vercel не дает выбрать Root Directory, и я помогу!

Или попробуйте Render.com - там проще для monorepo проектов.
